{
  "name": "toolbox",
  "private": true,
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "dev": "vite",
    "electron:dev": "concurrently vite \"pnpm main:build\" \"electron .\"",
    "build": "pnpm render:build && pnpm main:build && pnpm package",
    "main:build":"tsc -p  src/main --outDir dist",
    "render:build":"vue-tsc --noEmit && vite build",
    "preview": "vite preview --open",
    "package":"electron-builder"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "electron": "^18.0.0",
    "electron-builder": "^22.14.13",
    "@vitejs/plugin-vue": "^2.2.0",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "vue-tsc": "^0.29.8"
  },
  "build": {
    "appId": "com.toolbox.app", 
    
    "win": {
      "target": [
        "nsis",
        "zip"
      ]
    },

    "directories":{
      "output":"electron"
    }
   
  },
  "engines": {
    "node": ">=16"
  }
}

